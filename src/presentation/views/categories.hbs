<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Grocery Helper - Categorias</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://unpkg.com/htmx.org@1.9.10"></script>
</head>
<body class="bg-gray-50">
  <div class="min-h-screen">
    <nav class="bg-white shadow">
      <div class="max-w-7xl mx-auto px-4 py-4">
        <h1 class="text-2xl font-bold text-gray-900">🛒 Grocery Helper</h1>
        <div class="flex gap-4 mt-2">
          <a href="/categories" class="text-blue-600 hover:text-blue-800 font-semibold">Categorias</a>
          <a href="/stock-items" class="text-blue-600 hover:text-blue-800 font-semibold">Estoque</a>
          <a href="/shopping-lists" class="text-blue-600 hover:text-blue-800 font-semibold">Listas de Compras</a>
        </div>
      </div>
    </nav>

    <main class="max-w-7xl mx-auto px-4 py-8">
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <!-- Formulário de criar categoria -->
        <div class="lg:col-span-1">
          {{> category_form}}
        </div>

        <!-- Lista de categorias -->
        <div class="lg:col-span-2">
          <div id="categories-list" class="space-y-2">
            <h2 class="text-2xl font-bold text-gray-800 mb-4">Categorias</h2>
            {{#each categories}}
              {{> category_item}}
            {{/each}}
          </div>
        </div>
      </div>
    </main>
  </div>

  <script>
    // Configurar indicadores de carregamento do HTMX
    htmx.config.indicatorStyle = 'spinner';
    htmx.config.refreshOnHistoryMiss = true;
  </script>
</body>
</html>
